import 'package:flutter/material.dart';

class YearsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Années")),
      body: Center(child: Text("Liste des années ici")),
    );
  }
}
