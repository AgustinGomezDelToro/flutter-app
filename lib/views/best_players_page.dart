import 'package:flutter/material.dart';

class BestPlayersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Meilleurs joueurs")),
      body: Center(child: Text("Liste des meilleurs joueurs ici")),
    );
  }
}
